<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Current Finstagram Posts</title>
  </head>
  <body>
    <h1>Hello, <%= current_user.username %></h1>
    <br><br>

    <% @posts.each do |post| %>
      <a href="/users/<%= @post.user.slug %>"><%= @post.user.name%></a>
    <% end %>
  </body>
</html>

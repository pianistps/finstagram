<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title></title>
  </head>
  <body>
    <h1><% if @user && @user == current_user %>Your
      <% else %><%= @user.username %>'s
      <% end %>Profile</h1>

    <% posts = @user.posts.sort_by {|post| post.id }.reverse %>
    <% posts.each do |post| %>
    <a href="/users/<%= post.user.slug %>"><%= post.user.username %></a>
    <br><br>

    <a href="/posts/<%= post.id %>"><%= post.post_photo %></a>
    <p><%= post.caption %></p>
    <% end %>
    <br><br>

    <% if @user == current_user %><% else %><% end %>
    <form action="/users/<%= @user.id%>/edit" method="post">
      <input type="hidden" name="_method" value="edit">
      <input type="submit" name="" value="Edit Profile Photo">
    </form>

    <form action="/users/<%= @user.id%>/delete" method="post">
      <input type="hidden" name="_method" value="delete">
      <input type="submit" name="" value="Delete Profile">
    </form>
  </body>
</html>
